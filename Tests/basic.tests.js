const { tree } = require('../Tests/TestData/tree');
const invertBinaryTree = require('../Trees/InvertBinaryTree');

test('string with a single number should result in the number itself', () => {
  expect(invertBinaryTree(false)).toBe(false);
});
